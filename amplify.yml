version: 1
frontend:
  phases:
    build:
      commands:
        - echo skip
  artifacts:
    # Se publican solo los archivos del sitio; la carpeta infra (IaC) no se expone.
    baseDirectory: /
    files:
      - index.html
      - style.css
      - script.js
      - customHttp.yml
  cache:
    paths: []
